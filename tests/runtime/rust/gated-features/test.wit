package foo:bar@1.2.3;

interface iface {
  @unstable(feature = x)
  x: func();
  @unstable(feature = y)
  y: func();
  @since(version = 1.2.3)
  z: func();
}

world test {
  export iface;
}

world runner {
  import iface;
}
